import { renewalFilterPipe } from './renewal-filter.pipe';

describe('renewalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new renewalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
