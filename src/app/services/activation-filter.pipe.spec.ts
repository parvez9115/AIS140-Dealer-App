import { activationFilterPipe } from './activation-filter.pipe';

describe('activationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new activationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
