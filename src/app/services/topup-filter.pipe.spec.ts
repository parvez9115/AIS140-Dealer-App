import { topupFilterPipe } from './topup-filter.pipe';

describe('topupFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new topupFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
